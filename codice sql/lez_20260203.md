# Lezione del 3/2/2026

- SQL
  - DCL
  - DDL
  - DML
    - DQL

```sql
USE gas_2026;

show tables;

describe prova;
# CRUD

SHOW CREATE TABLE prova;

CREATE TABLE `prova` (
  `nome` varchar(30) DEFAULT NULL,
  `prezzo` decimal(6,2) DEFAULT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci;

# CREATE, 
# Creare nuovi record
INSERT INTO prova (nome, prezzo) VALUES ('Maglia rossa', 12);

# READ, 
SELECT nome, prezzo FROM prova;

# UPDATE, 
UPDATE prova SET prezzo = 15.50 WHERE prezzo = 12.00;

# DELETE
DELETE FROM prova WHERE prezzo = 12.00;

# modificare tabella
ALTER TABLE prova
ADD COLUMN descrizione VARCHAR(100);

DROP TABLE prova;    
```    
